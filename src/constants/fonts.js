import { StyleSheet } from 'react-native';

const fonts = StyleSheet.create({
  medium: "OSMedium",
  bold: "OSBold",
  regular: "OSRegular",
  light: "OSLight",
  semibold: "OSSemibold",
});

export default fonts